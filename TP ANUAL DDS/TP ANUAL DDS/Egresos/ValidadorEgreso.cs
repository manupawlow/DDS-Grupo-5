using System;
using System.Collections.Generic;
using System.Text;

namespace TP_ANUAL_DDS.Egresos
{
    class ValidadorEgreso
    {
        /*
        public bool egresoValido(Egreso egreso) 
        {
            if (egreso.cantpresupuesto == 0)
                return true;
            else
                cantidadCorrecta (egreso);

        }

        private bool cantidadCorrecta (Egreso egreso)
        {
            return egreso.cantPresupuestos == egreso.Presupuestos.Contains()
        }
        */
    }
}
