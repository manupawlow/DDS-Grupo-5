@{
    ViewBag.Title = "Ingreso";
}

<section id="services" class="bg-light">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 mx-auto">

                <h2>INGRESOS</h2>

                <h6></h6>
                <button onclick="myFunction(1)" target="_blank" class="button button2">Cargar nuevo ingreso</button>
                <div id="1" style="display:none;">

                    @using (Html.BeginForm("CargarIngreso", "Ingreso", FormMethod.Post))
                    {
                        <p><input type="text" name="descripcion" placeholder="Descripcion" class="button button3"></p>
                        <p><input type="text" name="total" placeholder="Total" class="button button3"></p>

                        <p><button id="cargarIngreso" class="button button4">Cargar ingreso</button></p>

                        <div id="ingresoInfo"></div>
                    }
                </div>

                <h6></h6>
                <button onclick="myFunction(2)" target="_blank" class="button button2">Asociar ingresos y egresos</button>

                <div id="2" style="display:none;">
                    <p>Seleccione el proceso de vinculacion</p>
                    @using (Html.BeginForm("FechaPrimerEgreso", "Ingreso", FormMethod.Post))
                    {
                        <button id="fechaPrimerEgreso" class="button button4">Fecha primer egreso</button>
                    }
                    @using (Html.BeginForm("ValorPrimerEgreso", "Ingreso", FormMethod.Post))
                    {
                        <button id="fechaPrimerEgreso" class="button button4">Valor primer egreso</button>
                    }
                    @using (Html.BeginForm("ValorPrimerIngreso", "Ingreso", FormMethod.Post))
                    {
                        <button id="fechaPrimerEgreso" class="button button4">Valor primer ingreso</button>
                    }


                </div>

            </div>
        </div>
    </div>
</section>
